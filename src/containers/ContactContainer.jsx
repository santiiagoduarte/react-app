import React from "react";

import PageTitle from '../components/PageTitle/index'

const ContactContainer = ({msg}) => {

    return (
        <PageTitle msgTitle={msg}></PageTitle>
    )
};

export default ContactContainer;